package com.renhejia.robot.launcher.displaymode.news;

import android.content.Context;
import android.support.annotation.Nullable;
import android.util.AttributeSet;

import com.renhejia.robot.launcher.displaymode.base.DisplayModeBaseView;

/**
 * 新闻界面
 *
 * @author liujunbin
 */
public class NewsView extends DisplayModeBaseView {

    public NewsView(Context context) {
        super(context);
    }

    public NewsView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public NewsView(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public NewsView(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
